
@model IEnumerable<ExamDomain.Model.ClosedAnswer>


<table class="table">

    @*<div class="form-group">

            <div class="col-md-3">
                @Html.DisplayFor(modelItem => item.AnswerText, v)
                @Html.TextBoxFor(m => m.AnswerText, new {@class = "form-control"})
            </div>

            @Html.DisplayFor(modelItem => item.IsCorrect, new {htmlAttributes = new {@class = "form-control"}})

        </div>*@


    <tr>
        <th>
            <div>
                @Html.DisplayNameFor(model => model.AnswerText)
            </div>
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IsCorrect)
        </th>
            
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td style="width: 20%">
                @Html.DisplayFor(modelItem => item.AnswerText, new { @class = "form-control" })
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IsCorrect)
            </td>

            @*<td>
                @Html.ActionLink("Edit", "Edit", new { id=item.ID }) |
                @Html.ActionLink("Details", "Details", new { id=item.ID }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.ID })
            </td>*@
        </tr>
    }

</table>
